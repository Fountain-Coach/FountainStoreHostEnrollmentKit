{
  "identity": "fountainstore.credential.provision",
  "version": "0.2.0",
  "operation": "fountainstore.credential.provision.remote",
  "owner": "Fountain-Coach",
  "preconditions": [
    "the exact target and enrolled host identity are declared",
    "the local SecretStore reference resolves to a non-empty credential",
    "the remote SecretStore reference is declared",
    "the request is unexpired and idempotent"
  ],
  "actions": [
    "retrieve the credential only through the host SecretStore adapter",
    "send it in memory through the authenticated host-agent transport",
    "store it under the remote SecretStore reference",
    "return a redacted terminal receipt"
  ],
  "terminalPredicates": [
    "receipt.terminal == true",
    "receipt.state == remote-provisioned",
    "credential value is absent from request, transport envelope, receipt, telemetry, and logs",
    "returned fingerprint matches the local credential fingerprint"
  ],
  "requiredEvidence": [
    "instrument identity and version",
    "authenticated exact-target host receipt",
    "remote SecretStore write receipt",
    "matching credential fingerprint",
    "redacted value-not-returned proof"
  ]
}
