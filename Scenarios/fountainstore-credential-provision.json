{
  "identity": "fountainstore.credential.provision",
  "version": "0.1.0",
  "owner": "Fountain-Coach",
  "preconditions": [
    "target is explicit",
    "SecretStore reference is explicit",
    "named SecretStore account is absent",
    "request is unexpired and idempotent"
  ],
  "actions": [
    "host adapter generates an opaque credential",
    "host adapter stores the credential in SecretStore",
    "instrument returns only a fingerprint and redacted evidence"
  ],
  "terminalPredicates": [
    "receipt.terminal == true",
    "receipt.state == generated-and-stored",
    "credential value is absent from request, receipt, telemetry, and logs"
  ],
  "requiredEvidence": [
    "instrument identity and version",
    "SecretStore write receipt",
    "credential fingerprint",
    "redacted value-not-returned proof"
  ]
}
